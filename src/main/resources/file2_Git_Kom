
Основные команды Git для терминала:

git commit -a -m "Сообщение"   # То же самое, что последовательное выполнение git add . и git commit -m "Сообщение коммита
git reset

git add .
git commit --amend -m "Сообщение коммита" # Дополняет (перезаписывает) последний коммит, добавляя в него "свежие" изменения.
                                            Также, меняет сообщение последнего коммита. Новый коммит не создается!

git reset head^     # Возвращает проект к указанному коммиту, при этом переводит
                      все коммиты после указанного в неотслеживаемую (unstaged)
                      зону. head^ - предыдущий head
git remote -v
git remote remove STICH
git remote add STICH git@github.com:darsiya77/stich.git

git branch AddHibernateInProject  # Добавление ветки AddHibernateInProject
git branch                        # Показывает текущую ветку, на которой находимся
git branch -d название вктки      # Удалить соответсвующую ветку
git checkout название ветки       # Переключиться на ветку с указанным названием
git merge название ветки          # слияние указанной ветки с главной веткой master/main

git push -u origin <ветка>        # Отправить изменения в удалённый репозиторий (в первый раз)
git push                          # Отправить изменения (после первого push)
git pull                          # Получить изменения из удалённого репозитория
git fetch                         # Загрузить изменения без слияния

git init
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/darsiya77/PP_2_1_4_spring-types-of-wiring.git
git push -u origin main
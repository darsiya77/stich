package Archive.Uroki.vetvleniya;

public class Vetvlenie2If {
    public static void main(String[] args) {
        int a=5;
        int b=1;
        boolean bool=true;
        if (!(a==b) || !bool){  // && - лог. оператор И; || - лог.оп.  ИЛИ
            System.out.print("Это истина!");
            String str1 = new String(" в пул?").intern();
            float f = Long.MAX_VALUE * Long.MAX_VALUE * Long.MAX_VALUE * Long.MAX_VALUE * Long.MAX_VALUE * Long.MAX_VALUE;
            System.out.println(f);
        }
    }

}
